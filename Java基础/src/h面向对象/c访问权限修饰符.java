package h面向对象;

public class c访问权限修饰符 {
    public static void main(String[] args) {
        /**修饰类、属性、方法，没有修饰符默认是包访问权限
         *                  private  default  protected    public
         *
         *同一个包的同一类     支持       支持    支持         支持
         *同一个包的不同类                支持    支持        支持
         *不同包的子类                           支持        支持
         * 不同包的非子类                                    支持
         */
    }
}
