package a基础知识点.b标识符和关键字;

public class demo1 {
    /**
     * 标识符的定义格式：字母、数字、下划线_、$,
     * 不能用数字开头，不能是Java关键字，要有意义。
     * 标识符区分大小写。
     */

    int a;
    int _b;
    int $b;
    //int 1a;
    //int class;
    //int 78.9;
    //int a b;空格不行

    /**
     * 关键字就是特殊的标识符。
     * 程序控制：if else while do switch case break for
     *          return instanceof default continue
     * 异常处理：try catch throw throws finally
     * 基本类型：byte short int long float double boolean char true false null
     * 包相关的：import package
     * 访问控制 private public protected
     * 类 方法 变量修饰符：class new final abstract extends implements
     *      native synchronized static interface volatile transient strictfp
     *      enum assert
     * 变量引用 super this void
     * 保留字 goto const
     *
     */
}
